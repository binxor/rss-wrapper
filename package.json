{
  "name": "rss-wrapper",
  "version": "1.0.0",
  "description": "A wrapper for rss-parser which provides extra fields",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "binxor",
  "license": "ISC",
  "dependencies": {
    "hapi": "^18.1.0",
    "remove-stopwords": "^1.0.3",
    "rss-parser": "^3.6.2"
  }
}
